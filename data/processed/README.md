# `data/processed/`

Cleaned tables produced by your code.

- **`matches.parquet`**: built from `data/raw/results.csv` via `python ml/etl_results_to_parquet.py` (see that script for column meanings).
