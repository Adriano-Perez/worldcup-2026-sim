# Data sources

## International match history (raw)

| File (in `data/raw/`) | Upstream |
|----------------------|----------|
| `results.csv` | [martj42/international_results](https://github.com/martj42/international_results) |
| `shootouts.csv` | same |
| `goalscorers.csv` | same |

Re-download or refresh from that repository if you need newer rows. Always keep the upstream **license** in mind before publishing or commercializing.

## 2026 World Cup fixtures

Not bundled here. Plan to add `fixtures_2026.csv` (or similar) from **official FIFA** schedule + your own `venues.csv` (coords, altitude, timezone) keyed by stadium/city.
